*, *:before, *:after {
  padding: 0;
  margin: 0;
}

body {
  background-image: scrim-gradient(to bottom, purple, transparent);
  background-position: center;
  background-repeat: no-repeat;
  background-size: cover;
  height: 100vh;
}
